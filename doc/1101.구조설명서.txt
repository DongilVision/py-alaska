╔══════════════════════════════════════════════════════════════════════════════╗
║  ALASKA v2.3 - 구조설명서                                                    ║
║  System Architecture Specification                                          ║
╠══════════════════════════════════════════════════════════════════════════════╣
║  Version : 2.3.0                                                             ║
║  Date    : 2026-02-28                                                        ║
╚══════════════════════════════════════════════════════════════════════════════╝


1. 시스템 개요
================================================================================

ALASKA v2.3은 초저지연(Ultra-low latency) 실시간 제어를 위한 멀티프로세스 태스크 
관리 프레임워크이다. v2.3에서는 모든 핵심 인프라(통신, 통계, 구독, 감시)를 
공유 메모리 자원(SmAsset)으로 통합하여 성능을 극대화하였다.

┌─────────────────────────────────────────────────────────────────────────────┐
│                          ALASKA v2.3 Architecture                           │
│                                                                             │
│  ┌──────────┐    ┌─────────────────┐    ┌──────────┐    ┌──────────┐        │
│  │  GConfig  │    │  SmAsset Family │    │  Signal   │    │  Monitor  │        │
│  │ 설정관리  │    │ (Shared Memory) │    │ 이벤트통신│    │ 웹모니터  │        │
│  └─────┬────┘    └────────┬────────┘    └─────┬────┘    └─────┬────┘        │
│        └──────────────────┴──────────────────┴──────────────────┘            │
│                                 │                                           │
│                          ┌──────┴──────┐                                    │
│                          │ TaskManager  │                                    │
│                          │  태스크 관리  │                                    │
│                          └──────┬──────┘                                    │
│                    ┌────────────┼────────────┐                              │
│              ┌─────┴─────┐ ┌───┴────┐ ┌─────┴─────┐                       │
│              │TaskRuntime│ │  RMI   │ │TaskRuntime│                        │
│              │ (thread)  │ │ Client │ │ (process) │                        │
│              └───────────┘ └────────┘ └───────────┘                        │
└─────────────────────────────────────────────────────────────────────────────┘


2. 핵심 모듈 정의
================================================================================

2.1 IPC (Inter-Process Communication)
--------------------------------------------------------------------------------

목적: 태스크 간 원격 메서드 호출 (RMI) 및 초저지연 동기화

핵심 클래스:
  - RmiClient      : 프로세스 간 RMI 호출 (Pipe 기반)
  - DirectClient   : 스레드 간 직접 호출 (Zero-IPC)
  - SmLockFreeEvent: 공유 메모리 스핀락 기반 초저지연 프로세스 통지 (New)

파일: task_manager.py, sm_lock_free_event.py


2.2 Signal (이벤트 통신 및 QoS)
--------------------------------------------------------------------------------

목적: Pub/Sub 패턴 기반 이벤트 브로드캐스트 및 실시간성 보장(QoS)

QoS 기능 (v2.3):
  - Priority       : 5단계 우선순위 멀티 큐 스케줄링
  - Deadline       : 처리 기한 초과 시 자동 폐기 및 에러 보고
  - Chain Trace    : 시그널 체인(A->B->C) 전체 지연 시간 추적

핵심 클래스:
  - SignalBroker     : 중앙 브로커 (SmSignalRegistry 연동)
  - SignalClient     : 태스크별 송수신 및 QoS 엔진
  - SmSignalRegistry : 전역 구독 비트맵 레지스트리 (O(1) 조회) (New)

파일: task_signal.py, sm_signal_registry.py


2.3 Task (태스크 및 자가 치유)
--------------------------------------------------------------------------------

목적: 태스크 생명주기 관리 및 하트비트 기반 자가 치유

자가 치유 (v2.3):
  - Watchdog       : SmSignalRegistry의 하트비트 감시
  - Auto-Restart   : 태스크 정지/좀비 감지 시 자동 재시작

파일: task_manager.py, task_decoration.py


2.4 SmAsset (공유메모리 통합 자원)
--------------------------------------------------------------------------------

목적: 전역 상태, 데이터, 통계를 공유 메모리에서 1μs 이내로 관리

┌─────────────────────────────────────────────────────────────────────────────┐
│  SmAsset Family                                                             │
│                                                                             │
│  1. 데이터 전달: SmBlock(이미지), SmRingBuffer(메시지), SmQueue(IPC)        │
│  2. 상태 제어: SmValue(스칼라), SmSignalRegistry(구독), SmLockFreeEvent(신호)│
│  3. 감시 통계: SmSignalStats(성능), SmBlackBox(세션 감시)                   │
└─────────────────────────────────────────────────────────────────────────────┘

핵심 클래스:
  - SmBlock         : 이미지 버퍼 풀 (Free-List, O(1))
  - SmSignalStats   : 실시간 시그널 성능 통계소 (New)
  - SmBlackBox      : 시그널 체인 세션 완료 보장 감시 (New)

파일: sm_block.py, sm_value.py, sm_signal_stats.py, sm_blackbox.py


3. 구현 구조
================================================================================

3.1 전체 파일 구성 (sm_memory 패키지 강화)
--------------------------------------------------------------------------------

┌──────────────────────────┬──────────────────────────────────────────────────┐
│ 파일                     │ 역할                                             │
├──────────────────────────┼──────────────────────────────────────────────────┤
│ sm_block.py              │ 이미지 블록 풀 관리 (SmBlock)                    │
│ sm_value.py              │ 고속 스칼라 값 공유 (SmValue)                    │
│ sm_ring_buffer.py        │ Lock-free 메시지 전달 (SmRingBuffer)             │
│ sm_queue.py              │ SharedMemory IPC 큐 (SmQueue)                    │
│ sm_signal_stats.py       │ 실시간 시그널 통계 집계 (SmSignalStats)          │
│ sm_signal_registry.py    │ 구독 비트맵 및 태스크 등록 (SmSignalRegistry)    │
│ sm_blackbox.py           │ 시그널 체인 세션 감시 (SmBlackBox)               │
│ sm_lock_free_event.py    │ 초저지연 스핀락 이벤트 (SmLockFreeEvent)         │
└──────────────────────────┴──────────────────────────────────────────────────┘


4. 성능 목표 (v2.3)
================================================================================

4.1 지연 시간 (Latency)
--------------------------------------------------------------------------------

  ┌──────────────────────────┬────────────┬────────────────────────────────────┐
  │ 작업                     │ 목표       │ 구현 방법                          │
  ├──────────────────────────┼────────────┼────────────────────────────────────┤
  │ Signal emit (구독조회)   │ < 0.001ms  │ SmSignalRegistry 비트맵 조회      │
  │ 통계 기록 (record)       │ < 0.005ms  │ SmSignalStats 직접 메모리 쓰기    │
  │ 프로세스 통지 (Event)    │ < 0.001ms  │ SmLockFreeEvent 스핀락            │
  │ BlackBox 세션 체크       │ < 0.01ms   │ SmBlackBox 슬롯 접근              │
  └──────────────────────────┴────────────┴────────────────────────────────────┘


5. 실시간 가시성 (Visibility)
================================================================================

5.1 SmAsset 모니터링
--------------------------------------------------------------------------------

  ALASKA v2.3의 모든 공유 메모리 자원은 웹 디버거에서 실시간 가시화된다.
  - Signal Stats: 각 시그널의 실시간 Transit/Proc Latency 차트 제공
  - BlackBox: 실행 중인 시그널 체인의 단계별 완료 현황 트리 뷰
  - Registry: 현재 살아있는 태스크와 구독 맵 Mermaid 다이어그램


================================================================================
  End of Document - 1101.구조설명서 (v2.3 Update)
================================================================================
